name: loan_tracker
description: A simple loan tracking app.
publish_to: 'none'

environment:
  sdk: '>=2.17.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  sqflite: ^2.3.0
  path: ^1.8.0

flutter:
  uses-material-design: true
